import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.gov.pkppapp',
  appName: 'PKPPApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
